#include <stdlib.h>

int *array_diff(const int *arr1, size_t n1, const int *arr2, size_t n2, size_t *z) {

    //  <----  hajim
    	bool isNumberInArray(int num, int *array, size_t arraySize){
    		for(size_t i =0; i<arraySize; ++i){
			if(array[i] == num){
				return (true);
			}
		}
		return (false);
    	}	

	
}
